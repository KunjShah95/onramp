[pytest]
asyncio_mode = auto
testpaths = tests
# Always put backend/ on sys.path so the local `tests` package wins over any
# third-party `tests` module installed in site-packages, regardless of CWD.
pythonpath = .
