apiVersion: apps/v1
kind: Deployment
metadata:
  name: onramp-redis
  labels:
    app: onramp
    component: redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: onramp
      component: redis
  template:
    metadata:
      labels:
        app: onramp
        component: redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          command:
            - redis-server
            - --appendonly
            - "yes"
            - --requirepass
            - "$(REDIS_PASSWORD)"
          ports:
            - containerPort: 6379
          env:
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: onramp-secrets
                  key: REDIS_PASSWORD
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            exec:
              command:
                - redis-cli
                - -a
                - "$(REDIS_PASSWORD)"
                - ping
            initialDelaySeconds: 10
            periodSeconds: 10
          volumeMounts:
            - name: redis-data
              mountPath: /data
      volumes:
        - name: redis-data
          persistentVolumeClaim:
            claimName: onramp-redis-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: onramp-redis
  labels:
    app: onramp
    component: redis
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: onramp
    component: redis
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: onramp-redis-pvc
  labels:
    app: onramp
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
