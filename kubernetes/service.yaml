apiVersion: v1
kind: Service
metadata:
  name: onramp-backend
  labels:
    app: onramp
    component: backend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app: onramp
    component: backend
