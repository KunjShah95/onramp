/*
 * Landing footer — thin re-export of the shared public SiteFooter.
 *
 * The landing page and every marketing sub-page now share ONE footer so the
 * sitemap, status bar, and brand block never drift between surfaces. The
 * implementation lives in components/layout/MarketingFooter.tsx.
 */
export { default } from '../layout/MarketingFooter'
